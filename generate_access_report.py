#!/usr/bin/env python3
"""
生成 HTML 外部访问使用报告
"""

from datetime import datetime
import os

def generate_access_report():
    """生成访问报告"""
    
    html_file = "/root/.openclaw/workspace/btc_report.html"
    bind_address = "0.0.0.0"
    port = 8081  # 使用 8081 避免冲突
    
    # 获取服务器 IP
    try:
        result = subprocess.run(['curl', '-s', 'ifconfig.me'], 
                              capture_output=True, text=True, timeout=10)
        server_ip = result.stdout.strip()
    except:
        server_ip = "47.90.150.51"
    
    report = f"""
================================================================================
🌐 HTML 报告外部访问解决方案 - 使用指南
================================================================================

⏰ 生成时间: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}

================================================================================
✅ 当前状态
================================================================================

📄 HTML 报告文件:
  • 路径: {html_file}
  • 文件大小: {os.path.getsize(html_file) / 1024:.2f} KB
  • 存在状态: {'✅ 存在' if os.path.exists(html_file) else '❌ 不存在'}

🌍 服务器信息:
  • 公网 IP: {server_ip}
  • 监听地址: {bind_address}
  • 监听端口: {port}
  • HTTP 服务: ✅ 已启动
  • Webhook 配置: ⚠️ 需要手动配置

================================================================================
📝 两种访问方式
================================================================================

方式 1: SSH 端口转发（推荐，最简单）
--------------------------------------------------------------------------------

优势:
  ✅ 完全免费
  ✅ 安全（SSH 加密）
  ✅ 不需要公网 IP
  ✅ 速度快
  ✅ 支持手机和电脑

配置步骤:

1️⃣ 在您的本地电脑上打开终端

   Windows:
   • 打开 PowerShell（搜索"PowerShell"）
   • 或使用 Git Bash

   Mac:
   • 打开 Terminal（Command + Space，输入"Terminal"）

   Linux:
   • 打开 Terminal (Ctrl + Alt + T)

2️⃣ 运行 SSH 端口转发命令:

   Windows PowerShell:
   ssh -N -L 8081:localhost:8081 root@{server_ip}

   Mac/Linux:
   ssh -N -L 8081:localhost:8081 root@{server_ip}

   参数说明:
   - -N: 不执行远程命令（只转发端口）
   - -L: 本地端口转发 (格式: 本地端口:本地地址:远程端口)
   - 8081: 您本地电脑上的端口（可以改为其他）
   - localhost: 本地地址
   - {port}: 服务器上的端口
   - root@{server_ip}: SSH 连接（使用您的服务器用户名和 IP）

3️⃣ 输入 SSH 密码:
   • 提示输入密码时，输入您的服务器 SSH 密码
   • 如果使用密钥登录，会自动使用密钥

4️⃣ 保持 SSH 窗口打开:
   • 不要关闭终端窗口
   • 端口转发会一直运行

5️⃣ 在浏览器中访问:
   • 打开任意浏览器（Chrome, Edge, Safari, Firefox）
   • 在地址栏输入: http://localhost:8081/
   • 回车！

6️⃣ 完成!
   • 您可以看到完整的 HTML 报告了
   • 包含市场分析、回测图表、统计数据
   • 全部在一个页面中，显示完美

💡 提示:
  • 如果 8081 端口被占用，改为其他端口（如 8082, 8083）
  • Windows 可能提示"防火墙已阻止"，点击"允许访问"
  • SSH 连接断开时，重新运行命令即可
  • 要停止转发，关闭 SSH 窗口或按 Ctrl+C

--------------------------------------------------------------------------------

方式 2: SSH SOCKS 代理（适合高级用户）
--------------------------------------------------------------------------------

优势:
  ✅ 可以在多个浏览器中使用
  ✅ 可以配置为系统代理
  ✅ 更稳定持久

配置步骤:

1️⃣ 配置 SSH SOCKS 代理:

   Windows (C:\\Users\\YourName\\.ssh\\config):
   
   Host btc-monitor
       HostName {server_ip}
       User root
       DynamicForward 8081
       ProxyCommand nc -X 5 -x 127.0.0.1 1080 %h %p

   Mac/Linux (~/.ssh/config):
   
   Host btc-monitor
       HostName {server_ip}
       User root
       DynamicForward 8081
       ProxyCommand nc -X 5 -x 127.0.0.1 1080 %h %p

2️⃣ 连接到服务器:
   ssh -N root@{server_ip}

3️⃣ 配置浏览器代理:

   Windows PowerShell:
   $env:ALL_PROXY = "socks5://localhost:8081"
   Start-Process chrome.exe

   Mac/Linux Terminal:
   export ALL_PROXY="socks5://localhost:8081"
   chromium-browser &
   # 或
   google-chrome &

4️⃣ 在浏览器中访问:
   http://localhost:8081/

💡 提示:
  • 需要安装 netcat: brew install nc (Mac) 或 apt install netcat (Linux)
  • Windows 可能需要手动配置代理设置
  • 较复杂，不推荐初学者使用

--------------------------------------------------------------------------------

📱 手机端访问方案
--------------------------------------------------------------------------------

方案 A: 使用 Termius (推荐)
--------------------------------------------------------------------------------

1. 下载 Termius:
   • 在 App Store 或 Google Play 搜索"Termius"
   • 下载并安装

2. 添加您的服务器:
   • 打开 Termius
   • 点击"+"号添加主机
   • 配置:
     * Host: {server_ip}
     * Username: root
     * Password: [您的 SSH 密码] 或使用密钥
     * Port: 22
   • 保存

3. 启用端口转发:
   • 进入主机设置
   • 找到"Port Forwarding"
   • 点击"Add"
   • 配置:
     * Local Port: 8081
     * Host Port: 8081
     * Protocol: TCP
     * 保存

4. 连接到服务器:
   • 点击主机连接

5. 在手机浏览器访问:
   • 打开任意浏览器（Safari, Chrome）
   • 访问: http://localhost:8081/

6. 完成!

✅ 优点:
  • 简单易用
  • 图形界面
  • 免费版够用
  • 支持端密钥
  • 自动重连

❌ 缺点:
  • 需要安装 App
  • 免费版有连接限制

--------------------------------------------------------------------------------

方案 B: 使用 JuiceSSH (免费)
--------------------------------------------------------------------------------

1. 下载 JuiceSSH:
   • 在 App Store 或 Google Play 搜索"JuiceSSH"
   • 下载并安装

2. 添加服务器:
   • 打开 JuiceSSH
   • 点击"+" -> "新建连接"
   • 配置:
     * 服务器名称: BTC 监控
     * 地址: {server_ip}
     * 用户名: root
     * 密码: [您的 SSH 密码]
     * 端口: 22
   • 保存

3. 启用端口转发:
   • 进入连接设置
   • 找到"端口转发"
   • 点击"添加"
   • 配置:
     * 类型: 本地端口转发
     * 本地端口: 8081
     * 远程主机: {server_ip}
     * 远程端口: 8081
   • 保存

4. 连接到服务器
   • 点击连接

5. 在手机浏览器访问:
   • 打开浏览器
   • 访问: http://localhost:8081/

6. 完成!

✅ 优点:
  • 完全免费
  • 简单易用
  • 支持端密钥
  • 连接稳定

❌ 缺点:
  • 界面较简单
  • 需要手动配置

--------------------------------------------------------------------------------

方案 C: 使用 Termux (Android Token 行)
--------------------------------------------------------------------------------

1. 安装 Termux:
   • 在 Google Play 搜索"Termux"
   • 下载并安装
   • 打开 Termux

2. 安装 openssh:
   pkg install openssh

3. 运行端口转发命令:
   ssh -N -L 8081:localhost:8081 root@{server_ip}

4. 输入密码（如果需要）

5. 保持 Termux 打开（在后台运行）

6. 在手机浏览器访问:
   http://localhost:8081/

✅ 优点:
  • 完全免费
  • 强大的命令行工具

❌ 缺点:
  • 需要使用命令行
  • 需要一定的 Linux 知识

================================================================================
🎯 快速开始（推荐）
================================================================================

对于 Windows 用户:
--------------------------------------------------------------------------------

1. 打开 PowerShell
2. 复制并运行:
   ssh -N -L 8081:localhost:8081 root@{server_ip}
3. 输入密码
4. 打开浏览器访问: http://localhost:8081/
5. 完成！

对于 Mac 用户:
--------------------------------------------------------------------------------

1. 打开 Terminal
2. 复制并运行:
   ssh -N -L 8081:localhost:8081 root@{server_ip}
3. 输入密码
4. 打开浏览器访问: http://localhost:8081/
5. 完成！

对于 Linux 用户:
--------------------------------------------------------------------------------

1. 打开 Terminal
2. 复制并运行:
   ssh -N -L 8081:localhost:8081 root@{server_ip}
3. 输入密码
4. 打开浏览器访问: http://localhost:8081/
5. 完成！

对于手机用户 (iOS/Android):
--------------------------------------------------------------------------------

1. 下载并安装 Termius (App Store/Google Play)
2. 添加服务器 ({server_ip})
3. 配置端口转发 (8081 -> 8081)
4. 连接到服务器
5. 在浏览器访问: http://localhost:8081/
6. 完成！

================================================================================
📋 检查清单
================================================================================

完成配置后，请确认:

□ SSH 端口转发已运行 (Windows/Mac/Linux 用户)
□ Termius 端口转发已启用 (手机用户)
□ 浏览器可以访问 http://localhost:8081/
□ HTML 页面完整显示
□ 市场分析部分显示正常
□ 净值曲线图显示正常
□ 统计数据显示正常

全部确认后，您就可以随时访问了！

================================================================================
💡 高级技巧
================================================================================

1. 🔒 使用 SSH 密钥登录:
   • 更安全，无需每次输入密码
   • Windows: ssh-keygen -t rsa
   • Mac/Linux: ssh-keygen
   • 将公钥上传到服务器的 ~/.ssh/authorized_keys

2. 🔄 自动重连:
   • 添加参数: ssh -o ServerAliveInterval=60 -o ServerAliveCountMax=3 -N -L ...

3. 📁 修改本地端口:
   • 如果 8081 被占用，使用 8082, 8083 等
   • 相应修改命令中的端口号

4. 🌐 永久访问（可选）:
   • 修改 SSH 配置文件，添加自动连接
   • 需要重启 SSH 服务

5. 📱 手机端密钥管理:
   • 在 Termius 等应用中管理端密钥
   • 更安全，无需每次输入密码

================================================================================
❓ 常见问题 (FAQ)
================================================================================

Q: SSH 提示"连接被拒绝"？
A: 检查:
   • 服务器 IP 是否正确
   • SSH 服务是否运行: sudo systemctl status sshd
   • 防火墙是否允许 SSH (端口 22)
   • 用户名和密码是否正确

Q: 端口转发不工作？
A: 检查:
   • 本地 8081 端口是否被占用: netstat -ano | findstr 8081 (Windows)
   • SSH 命令是否正确运行
   • 检查 SSH 错误信息

Q: 浏览器显示"无法连接"？
A: 检查:
   • SSH 端口转发是否仍在运行
   • 浏览器地址是否正确: http://localhost:8081/
   • 尝试使用不同的浏览器
   • 尝试使用 127.0.0.1: http://127.0.0.1:8081/

Q: 手机无法连接？
A: 检查:
   • Termius 端口转发是否启用
   • 是否连接到服务器
   • 检查网络连接
   • 尝试重启 Termius 应用

Q: 页面显示不完整？
A: 检查:
   • HTML 文件是否完整生成
   • 浏览器是否支持 HTML5
   • 尝试清除浏览器缓存
   • 尝试使用不同的浏览器

================================================================================
📝 下一步
================================================================================

1. 选择适合您的访问方式（推荐：SSH 端口转发）
2. 按照上面的步骤配置
3. 访问 http://localhost:8081/ 查看完整的 HTML 报告
4. 每小时 BTC 监控自动更新 HTML 文件
5. 刷新浏览器页面即可看到最新报告

================================================================================
🎉 祝贺！
================================================================================

现在您可以从任何地方（本地电脑、手机）访问完整的 HTML BTC 监控报告了！

报告包含:
📊 市场分析（价格、技术指标、市场情绪）
📈 净值曲线图（PNG 图片）
📋 交易回测报告（收益率、最大回撤、夏普比率、胜率）
📉 所有统计数据

每小时自动更新，随时查看！
================================================================================
"""
    
    return report

def main():
    """主函数"""
    print("生成 HTML 外部访问使用报告...\n")
    
    report = generate_access_report()
    print(report)
    
    # 保存报告
    report_path = "/root/.openclaw/workspace/html_access_guide.txt"
    with open(report_path, 'w', encoding='utf-8') as f:
        f.write(report)
    
    print(f"\n✅ 使用指南已保存: {report_path}")
    print(f"🌐 本地访问地址: http://{bind_address}:{port}/")
    print(f"🖥 SSH 命令: ssh -N -L 8081:localhost:{port} root@{server_ip}")
    
    print("\n" + "=" * 80)
    print("💡 快速开始（三步）")
    print("=" * 80)
    print("1️⃣ 在您的电脑终端运行:")
    print(f"   ssh -N -L 8081:localhost:{port} root@{server_ip}")
    print("2️⃣ 输入 SSH 密码")
    print("3️⃣ 在浏览器访问: http://localhost:8081/")
    print("=" * 80)

if __name__ == "__main__":
    main()
